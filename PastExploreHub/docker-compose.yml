services:
  db:
    image: mysql:8.0
    container_name: mysql-db
	restart: always
    environment:
      MYSQL_DATABASE: pastexplorehub
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  app:
    image: docker2678/pastexplorehub:1.0
    container_name: pastexplorehub-app
    ports:
      - "8080:8080"
	restart: always
	depends_on:
      db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/pastexplorehub?allowPublicKeyRetrieval=true&useSSL=false
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
    healthcheck:
      # Pinging your login API is the most reliable health check
      test: ["CMD", "curl", "-f", "http://localhost:8080/user-api/login"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

volumes:
  mysql_data: